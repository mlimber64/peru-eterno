import 'package:flutter/material.dart';

class AppColors {
  // ── Brand ─────────────────────────────────────────────────────────────────
  static const Color ocre          = Color(0xFFC8860A);
  static const Color terracota     = Color(0xFFC1440E);
  static const Color verdeAndino   = Color(0xFF2D6A4F);
  static const Color cremaPergamino= Color(0xFFF5E6C8);
  static const Color marronOscuro  = Color(0xFF3E1C00);
  static const Color bgColor       = Color(0xFFF9F0DC);

  // ── Dark cinematic base ───────────────────────────────────────────────────
  static const Color negoCacao     = Color(0xFF1A0F0A);
  static const Color marronProfundo= Color(0xFF2C1810);
  static const Color overlayDark   = Color(0xCC000000); // 80% black

  // ── Era accent colors ─────────────────────────────────────────────────────
  static const Color caralColor        = Color(0xFFD4A854);
  static const Color mocheColor        = Color(0xFFC1440E);
  static const Color tiahuanacoColor   = Color(0xFF4A7C9E);
  static const Color incaColor         = Color(0xFFC8860A);
  static const Color conquistaColor    = Color(0xFF7A3B1E);
  static const Color virreinatoColor   = Color(0xFF5C3317);
  static const Color independenciaColor= Color(0xFF2D6A4F);

  // ── World accent colors ───────────────────────────────────────────────────
  static const Color worldHistoria   = Color(0xFFC1440E);
  static const Color worldCultura    = Color(0xFF2D6A4F);
  static const Color worldSabores    = Color(0xFFC8860A);
  static const Color worldMusica     = Color(0xFF6B3FA0);
  static const Color worldTerritorios= Color(0xFF1A5276);
  static const Color worldPersonajes = Color(0xFF8B4513);

  // ── Text ──────────────────────────────────────────────────────────────────
  static const Color textPrimary   = Color(0xFF2C1810);
  static const Color textSecondary = Color(0xFF6B4226);
  static const Color textOnDark    = Color(0xFFF5E6C8);
  static const Color textOnDarkMuted = Color(0xAAF5E6C8);

  // ── UI elements ───────────────────────────────────────────────────────────
  static const Color dividerColor  = Color(0xFFD4B896);
  static const Color cardShadow    = Color(0x303E1C00);
  static const Color shimmerBase   = Color(0xFF2A1A0E);
  static const Color shimmerHighlight = Color(0xFF3D2510);
}
